~ start
match get_tree().current_scene.name:
	when "home":
		do PlayerData.spawn = Vector3(5, 1.5, 0)
		do SceneManager.day_start = false
	when "church":
		do PlayerData.spawn = Vector3(-5, 1.5, 0)
do PlayerData.location = "town"
do SceneManager.change_scene("res://scenes/town.tscn")
=> END